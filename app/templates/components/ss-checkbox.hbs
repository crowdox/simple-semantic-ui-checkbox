<input type="checkbox"
       disabled={{if disabled "disabled"}}
       class="hidden"
       checked={{if checked "checked"}}
       indeterminate={{indeterminate}}/>
{{#if (has-block)}}
  {{yield}}
{{else}}
  <label></label>
{{/if}}
